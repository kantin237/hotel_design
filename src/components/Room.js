import React from 'react';
import {Link} from 'react-router-dom';
export default function Room({room}){
   // console.log(room);
    return (
        <div>
            hello from room
        </div>
    );
}